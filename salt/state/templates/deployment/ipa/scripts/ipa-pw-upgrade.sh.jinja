#!/bin/bash
# This script 'upgrades' plaintext passwords (from installation-time) to encrypted passwords

. /usr/local/bin/lib-ss.sh || exit 1
. /usr/local/bin/lib-ipa.sh || die "Failed loading ipa lib"

ipa::upgrade-passwords
