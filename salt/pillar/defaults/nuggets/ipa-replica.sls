nuggets:
    ipa-replica:
        description: |
            provides support for rolling out an IPA server replica (non-initial install)

        install:
            nuggets-required:
                - ipa-server
